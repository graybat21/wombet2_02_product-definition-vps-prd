# 프로토타입 제작용 제공 문서 가이드

Figma AI, v0.dev, Google AI Studio 등 AI 기반 UI/UX 제너레이터를 활용해 기능 구현(로직) 없이 대략적인 디자인과 화면 연계(프로토타입)를 빠르게 뽑아내려면, **"사용자 흐름(User Flow)"**과 **"화면별 컴포넌트 구성(Layout)"**에 집중된 문서를 제공하는 것이 가장 효과적입니다.

제안드리는 문서는 아래 **3가지**입니다. 

---

### 1. `05_Task/03_UI_TASK_상세명세서.md` (필수 ⭐⭐⭐)
- **제공 이유**: AI가 각 화면에 **어떤 UI 컴포넌트(버튼, 이미지 슬롯, 텍스트 폼 등)를 어떻게 배치해야 하는지**에 대한 가장 구체적인 지시어(Prompt) 역할을 합니다.
- **활용 포인트**: UI-001(공통 레이아웃)부터 UI-006(진단 대기 상태 UI)까지 화면 단위로 쪼개져 있어, AI에게 "UI-002 페이크도어 랜딩페이지를 만들어줘"라고 요청할 때 이 문서의 내용을 그대로 붙여넣기 좋습니다.

### 2. `05_Task/02_보완된_UI_TASK_리스트.md` (권장 ⭐⭐)
- **제공 이유**: 단순히 개별 화면을 만드는 것을 넘어, **화면 간의 연계성(조립 시나리오)**을 AI에게 이해시킬 수 있습니다.
- **활용 포인트**: 문서 하단의 `조립 시나리오` 파트를 보면 "상세 페이지(UI-004) 상단에 네거티브 경고 박스(F1-004)가 삽입된다"는 식으로 전체적인 그림이 설명되어 있습니다. AI가 화면 간의 관계(Depth)와 플로우를 파악하여 프로토타입을 연결하는 데 큰 도움을 줍니다.

### 3. `04_SRS-Drafts/SRS_v0.3_by_gemini.md` (참고 ⭐)
- **제공 이유**: 서비스의 목적과 전체적인 **사용자 여정(User Journey)**을 AI에게 학습시키기 위한 Context용 문서입니다.
- **활용 포인트**: 전체를 다 넣으면 토큰 낭비 및 환각(Hallucination)이 올 수 있으므로, 문서 내에서 **`3.4 Dynamic Architecture (Sequence / Activity Flow)`** 부분과 **`6.3 Detailed Interaction Models`** 부분만 발췌해서 제공하는 것을 추천합니다. AI가 "아, 이 버튼을 누르면 다음엔 이 화면으로 가야 하는구나"를 이해하게 됩니다.

---

### 💡 다온의 AI 프롬프팅 팁 (Figma AI 등 사용 시)

AI에게 문서를 제공할 때 한 번에 모두 던져주기보다는 아래 순서대로 요청하시는 것이 퀄리티가 훨씬 높게 나옵니다.

1. **컨텍스트 주입**: `SRS_v0.3_by_gemini.md`의 핵심 요약과 `02_보완된_UI_TASK_리스트.md`를 먼저 주며 *"우리는 스포츠 부상 방지를 위한 신발 추천 서비스 MVP를 만들 거야. 전체 화면 흐름을 먼저 이해해 줘."* 라고 학습시킵니다.
2. **페이지별 생성**: `03_UI_TASK_상세명세서.md`의 Task를 하나씩(예: UI-002 랜딩페이지) 주면서 *"이 Task Breakdown에 맞춰서 랜딩페이지 와이어프레임을 그려줘"* 라고 분할 요청합니다.
3. **디자인 시스템 입히기**: *"전반적으로 신뢰감을 주는 푸른색 계열과, 네거티브 경고 요소에는 눈에 띄는 붉은색을 사용해서 디자인해 줘"* 등 구체적인 톤앤매너를 추가 지시합니다.
